<mvc:View
        displayBlock="true"
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc">

    <VBox items="{appModel>/code}">
        <items>
           <Text text="{appModel>}" />
        </items>
    </VBox>

</mvc:View>
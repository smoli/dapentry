<mvc:View
        controllerName="sts.drawable.controller.Library"
        displayBlock="true"
        xmlns="sap.m"
        xmlns:unified="sap.ui.unified"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns:sts="sts.drawable.controls.color"
>
    <Title text="Library" />
    <VBox items="{appModel>/library}">
        <Button text="{appModel>name}" press="onLibraryEntrySelected" />
    </VBox>

</mvc:View>
<mvc:View
        displayBlock="true"
        xmlns="sap.m"
        xmlns:l="sap.ui.layout"
        xmlns:code="sap.ui.codeeditor"
        xmlns:step="sts.drawable.controls.steps"
        xmlns:mvc="sap.ui.core.mvc">

    <l:Splitter orientation="Vertical">
        <l:contentAreas>

        <l:Splitter orientation="Horizontal">
            <l:contentAreas>
                <l:Splitter orientation="Vertical">
                    <l:contentAreas>

                        <VBox>
                            <layoutData>
                                <l:SplitterLayoutData size="{appModel>/dataEditorSize}"/>
                            </layoutData>
                            <mvc:XMLView viewName="sts.drawable.view.DataEditor"/>
                        </VBox>

                        <mvc:XMLView viewName="sts.drawable.view.StepList"/>

                        <code:CodeEditor value="{appModel>/codeString}" change="onCodeChanged" syntaxHints="false">
                            <code:layoutData>
                                <l:SplitterLayoutData size="{= ${appModel>/codeEditor} ? ${appModel>/codeEditorSize} : '0px'}" resizable="{appModel>/codeEditor}"/>
                            </code:layoutData>
                        </code:CodeEditor>


                    </l:contentAreas>
                </l:Splitter>

                <VBox>
                    <layoutData>
                        <l:SplitterLayoutData size="65%"/>
                    </layoutData>
                    <mvc:XMLView viewName="sts.drawable.view.Drawing"/>
                </VBox>

                <VBox>
                    <layoutData>
                        <l:SplitterLayoutData size="{= ${appModel>/styleEditor} ? '300px' : '0px'}" resizable="false"/>
                    </layoutData>

                    <mvc:XMLView viewName="sts.drawable.view.Style"/>
                </VBox>

            </l:contentAreas>
        </l:Splitter>

            <Toolbar>
                <layoutData>
                    <l:SplitterLayoutData size="2rem" resizable="false"/>
                </layoutData>
                <ToggleButton text="Code" pressed="{appModel>/codeEditor}"/>
                <ToolbarSpacer/>
                <ToggleButton text="Style" pressed="{appModel>/styleEditor}" />

            </Toolbar>
        </l:contentAreas>
    </l:Splitter>

</mvc:View>
